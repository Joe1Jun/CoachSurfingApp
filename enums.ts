//export enum to be used in the main index.ts

export enum Permissions {
    ADMIN = 'ADMIN', 
    READ_ONLY = 'READ_ONLY'
}

export enum LoyaltyLevels {
    GOLD_USER,
    SILVER_USER,
    BRONZE_USER

}